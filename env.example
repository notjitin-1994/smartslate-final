# AWS Cognito (to be configured)
# COGNITO_USER_POOL_ID="your_user_pool_id"
# COGNITO_APP_CLIENT_ID="your_app_client_id"
# COGNITO_REGION="your_region"

# Database (to be provided by AWS)
# DATABASE_URL="postgresql://username:password@host:port/database"

# JWT secret for local dev (temporary)
# JWT_SECRET="replace-with-strong-secret"

# Stack/Neon variables removed

# Node Environment
NODE_ENV="development"

# Optional canonical host enforcement (set in production only if needed)
# Example: smartslate.io OR www.smartslate.io
CANONICAL_HOST=""
